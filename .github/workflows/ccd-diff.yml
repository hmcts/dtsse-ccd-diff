name: Diff CCD definition

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-java@v3
        with:
          distribution: corretto
          java-version: 17

  buildBranch:
    runs-on: ubuntu-latest
    outputs:
      path: ${{ steps.definition.outputs.path }}
    needs: [setup]
    steps:
      - uses: actions/checkout@v3
        with:
          path: build/branch
          repository: hmcts/nfdiv-case-api # TODO remove
          ref: nfdiv-date-picker # TODO remove
      - name: Build
        run: ./gradlew generateCCDConfig
        working-directory: build/branch
      - name: Set JSON path
        id: definition
        run: echo "::set-output name=path::build/branch/build/definitions/NFD"

  buildMaster:
    runs-on: ubuntu-latest
    outputs:
      path: ${{ steps.definition.outputs.path }}
    needs: [setup]
    steps:
      - uses: actions/checkout@v3
        with:
          ref: master
          path: build/master
          repository: hmcts/nfdiv-case-api # TODO remove
      - name: Build
        run: ./gradlew generateCCDConfig
        working-directory: build/master
      - name: Set JSON path
        id: definition
        run: echo "::set-output name=path::build/master/build/definitions/NFD"

  report:
    runs-on: ubuntu-latest
    needs: [buildBranch, buildMaster]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: 16
      - name: Generate report
        run: |
          echo "::set-output name=content::$(npm start ${{needs.buildMaster.outputs.path}} ${{needs.buildBranch.outputs.path}})')"
        id: report
      - name: Add report
        uses: thollander/actions-comment-pull-request@v1
        with:
          message: 'CCD diff ${{steps.report.content}}'
          comment_includes: 'CCD diff'
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
